name: fondeadora
description: App de Frase del Día.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^5.0.0
  email_validator: ^2.0.1
  intl: ^0.17.0
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0

flutter_icons:
  android: "launcher_icon"
  ios: false
  image_path: "images/icon.jpg"

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  assets:
    - images/mastercard.png
    - images/card.png
  